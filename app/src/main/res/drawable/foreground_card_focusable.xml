<?xml version="1.0" encoding="utf-8"?>
<!-- Layered foreground: ripple for touch + focus ring for keyboard navigation -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Ripple for touch feedback -->
    <item>
        <ripple android:color="@color/colorPrimaryContainer">
            <item android:id="@android:id/mask">
                <shape android:shape="rectangle">
                    <corners android:radius="@dimen/radius_card" />
                    <solid android:color="@color/surfaceColor" />
                </shape>
            </item>
        </ripple>
    </item>
    <!-- Focus ring shown only when focused via keyboard/d-pad -->
    <item>
        <selector>
            <item android:state_focused="true"
                android:drawable="@drawable/bg_focus_ring" />
        </selector>
    </item>
</layer-list>
