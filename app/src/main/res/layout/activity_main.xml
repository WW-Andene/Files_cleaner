<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/surfaceBase">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/surfaceBase"
        app:elevation="0dp">

        <!-- Clean header with app branding -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingHorizontal="20dp"
            android:paddingTop="12dp"
            android:paddingBottom="4dp">

            <ImageView
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:src="@drawable/ic_raccoon_face"
                android:importantForAccessibility="no" />

            <TextView
                android:id="@+id/toolbar_title"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:text="@string/app_name"
                android:textAppearance="@style/TextAppearance.FileCleaner.Title"
                android:textColor="@color/textPrimary" />
        </LinearLayout>

        <!-- Scan status bar — soft container style -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="16dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="4dp"
            app:cardCornerRadius="@dimen/radius_card"
            app:cardElevation="0dp"
            app:cardBackgroundColor="@color/scanBarBackground"
            app:strokeWidth="0dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingHorizontal="16dp"
                android:paddingTop="10dp"
                android:paddingBottom="6dp">

                <!-- Top row: icon + status text + cancel button -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <ImageView
                        android:layout_width="18dp"
                        android:layout_height="18dp"
                        android:src="@drawable/ic_scan"
                        android:importantForAccessibility="no"
                        app:tint="@color/colorPrimary" />

                    <TextView
                        android:id="@+id/tv_scan_status"
                        android:layout_width="0dp"
                        android:layout_weight="1"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="10dp"
                        android:text="@string/scan_prompt"
                        android:textAppearance="@style/TextAppearance.FileCleaner.BodySmall"
                        android:textColor="@color/scanBarText"
                        android:fontFamily="sans-serif-medium" />

                    <!-- Percentage text shown during scanning -->
                    <TextView
                        android:id="@+id/tv_scan_percent"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="8dp"
                        android:textAppearance="@style/TextAppearance.FileCleaner.Caption"
                        android:textColor="@color/colorPrimary"
                        android:fontFamily="sans-serif-medium"
                        android:visibility="gone" />

                    <ImageButton
                        android:id="@+id/btn_cancel_scan"
                        android:layout_width="28dp"
                        android:layout_height="28dp"
                        android:layout_marginStart="8dp"
                        android:src="@android:drawable/ic_menu_close_clear_cancel"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:contentDescription="@string/scan_cancel"
                        android:visibility="gone"
                        app:tint="@color/textSecondary" />
                </LinearLayout>

                <!-- Material LinearProgressIndicator: shown during scanning -->
                <com.google.android.material.progressindicator.LinearProgressIndicator
                    android:id="@+id/scan_progress_indicator"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:visibility="gone"
                    app:indicatorColor="@color/colorPrimary"
                    app:trackColor="@color/colorPrimaryContainer"
                    app:trackCornerRadius="2dp"
                    app:trackThickness="4dp" />

                <!-- Phase step indicators: dot row showing scan stages -->
                <LinearLayout
                    android:id="@+id/scan_phase_row"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:layout_marginTop="6dp"
                    android:visibility="gone"
                    android:gravity="center_vertical">

                    <TextView
                        android:id="@+id/tv_phase_label"
                        android:layout_width="0dp"
                        android:layout_weight="1"
                        android:layout_height="wrap_content"
                        android:textAppearance="@style/TextAppearance.FileCleaner.Caption"
                        android:textColor="@color/textTertiary" />

                    <TextView
                        android:id="@+id/tv_phase_step"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textAppearance="@style/TextAppearance.FileCleaner.Caption"
                        android:textColor="@color/textTertiary" />
                </LinearLayout>
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>
    </com.google.android.material.appbar.AppBarLayout>

    <!-- Fragment container -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_graph" />

    <!-- FAB scan button -->
    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
        android:id="@+id/fab_scan"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="80dp"
        android:text="@string/scan_storage"
        app:icon="@drawable/ic_scan"
        android:backgroundTint="@color/colorPrimary"
        android:textColor="@color/textOnPrimary"
        app:iconTint="@color/textOnPrimary"
        app:shapeAppearance="@style/ShapeMedium"
        app:elevation="4dp" />

    <!-- Raccoon bubble: draggable floating toggle for tree view -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/raccoon_bubble_card"
        android:layout_width="52dp"
        android:layout_height="52dp"
        android:layout_gravity="bottom|start"
        android:layout_marginStart="16dp"
        android:layout_marginBottom="80dp"
        app:cardCornerRadius="26dp"
        app:cardElevation="6dp"
        app:cardBackgroundColor="@color/surfaceColor"
        app:strokeWidth="2dp"
        app:strokeColor="@color/colorPrimaryContainer">

        <ImageView
            android:id="@+id/raccoon_bubble"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_raccoon_face"
            android:contentDescription="@string/open_arborescence"
            android:importantForAccessibility="yes" />
    </com.google.android.material.card.MaterialCardView>

    <!-- Bottom nav -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@color/bottomNavBackground"
        app:menu="@menu/bottom_nav_menu"
        app:labelVisibilityMode="labeled"
        app:itemIconTint="@color/bottom_nav_color"
        app:itemTextColor="@color/bottom_nav_color"
        app:elevation="8dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
